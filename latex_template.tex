% to insert figures and tables
\usepackage{graphicx}

% to set font and to allow unicode
\usepackage{fontspec}
\setmainfont{Helvetica}

% allows custom margins
\usepackage[margin=0.5in]{geometry}

% allow math equations and symbols
\usepackage{amsmath}
\usepackage{amssymb}

% allow custom line spacing 
\usepackage{setspace}
\singlespacing

% layout with zero indentation of first line and non-zero inter-para space
\usepackage{parskip}

% bibliography styling
\usepackage[comma,sort&compress]{natbib}

% allow table across multiple pages
\usepackage{longtable}

% allow custom colors including in tables
\usepackage[table]{xcolor}

% control display of hyperlinks
\usepackage[linktocpage, pdfstartview=FitH, colorlinks, linkcolor=blue, anchorcolor=blue, citecolor=blue, filecolor=blue, menucolor=blue, urlcolor=brown]{hyperref}

% to find last page
\usepackage{lastpage}

% to change date format YYYY-MM-DD
\usepackage{datetime2}

% allow custom header and footer
\usepackage{fancyhdr}
\pagestyle{fancy}

% allow underlining
\usepackage[normalem]{ulem}
  
\fancyhf{} % remove everything first
\renewcommand{\headrulewidth}{0pt} % remove ruler on head
% \renewcommand{\footrulewidth}{0.5pt} % add ruler below foot
% \fancyhf[FRE,FRO]{\thepage\ of \pageref*{LastPage}}
\fancyhf[FCE,FCO]{\thepage}
% \fancyhf[FLE,FLO]{\today}

% adjust look of title, author, date block
\usepackage{titling}
\pretitle{\begin{center}\bfseries\large}
\posttitle{\end{center}\vskip 0em}
\preauthor{\begin{center}\vskip 0em \bfseries\normalsize}
\postauthor{\end{center}\vskip -1em}
\predate{\begin{center}\vskip 0em \bfseries\normalsize}
\postdate{\end{center}\vskip -5em}
\setlength{\droptitle}{-40pt}

% adjust look of section, subsection titles
\usepackage{titlesec}
\titleformat{\section}[hang]{\bfseries}{\thesection.}{0.2em}{}
\titlespacing*{\section}{0pt}{-0pt}{-0pt}
\titleformat{\subsection}[hang]{\bfseries}{\thesubsection.}{0em}{}
\titlespacing*{\subsection}{0pt}{0pt}{0pt}